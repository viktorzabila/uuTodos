/* eslint-disable */
const sysDropDataStoresDtoInType = shape({
  dataStoreNames: array(string(), 1, null),
  recreateIndexes: boolean()
});

const sysStatsListDtoInType = shape({
  awidList: array(hexa32Code()),
  pageInfo: shape({
    pageIndex: integer(),
    pageSize: integer()
  })
});

const sysStatsCalculateDtoInType = shape({
  force: boolean()
});

const sysStatsPrivilegedCalculateDtoInType = shape({
  awidList: array(hexa32Code(), 1000),
  force: boolean()
});
