const oidcInitOidcClientDtoInType = shape({
  awid: id(),
  clientId: id(),
  providerName: string(),
  clientSecret: string(32, 256)
});

const oidcSetAllowedRedirectsDtoInType = shape({
  awid: id(),
  redirectUriList: array(uri(), 1, 16),
  postLogoutUriList: array(uri(), 0, 16),
  providerName: string()
});

const oidcGetAllowedRedirectsDtoInType = shape({
  awid: id(),
  providerName: string()
});
