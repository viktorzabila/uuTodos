"use strict";

const ObjectStoreError = require("./objectstore-error.js");

class ExpiredLock extends ObjectStoreError {
  constructor(cause, message) {
    super(message || "UuObject lock expired.", cause);
    this.code = `${ObjectStoreError.ERROR_PREFIX}expiredLock`;
  }
}

module.exports = ExpiredLock;
