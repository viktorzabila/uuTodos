"use strict";

const ObjectStoreError = require("./objectstore-error.js");

class ObjectLocked extends ObjectStoreError {
  constructor(cause, message) {
    super(message || "UuObject is locked.", cause);
    this.code = `${ObjectStoreError.ERROR_PREFIX}objectLocked`;
  }
}

module.exports = ObjectLocked;
