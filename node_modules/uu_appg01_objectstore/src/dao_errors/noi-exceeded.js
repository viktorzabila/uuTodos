"use strict";

const ObjectStoreError = require("./objectstore-error.js");

class NoiExceeded extends ObjectStoreError {
  constructor(cause, message) {
    super(message || "Number of schema instances exceeded.", cause);
    this.code = `${ObjectStoreError.ERROR_PREFIX}noiExceeded`;
  }
}

module.exports = NoiExceeded;
