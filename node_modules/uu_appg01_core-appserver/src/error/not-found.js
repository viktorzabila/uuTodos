"use strict";

const UseCaseError = require("./use-case-error.js");

class NotFound extends UseCaseError {
  constructor(msg, cause = null) {
    super(msg, cause);
    this.status = 404;
    this.code = `${UseCaseError.ERROR_PREFIX}server/resourceNotFound`;
  }
}

module.exports = NotFound;
