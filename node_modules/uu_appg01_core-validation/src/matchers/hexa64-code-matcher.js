"use strict";

const Matcher = require("../matcher.js");
const ValidationResult = require("../validation-result.js");
const Validator = require("../validator.js");

const HEXA64_CODE_REGEXP = /^[0-9a-f]{64}$/;

class _Hexa64Code extends Matcher {

  constructor() {
    super("hexa64Code");
  }

  match(value, ctx) {
    let result = new ValidationResult();
    if (value == undefined) {
      return result;
    }

    if (typeof value === "string" || value instanceof String) {
      if (!value.match(HEXA64_CODE_REGEXP)) {
        result.addInvalidValueError("hexa64Code.e001", "The value must be 64 characters long hexadecimal code.");
      }
    } else {
      result.addInvalidTypeError("hexa64Code.e001", "The value must be 64 characters long hexadecimal code.");
    }

    return result;
  }

}

function Hexa64Code() {
  let matcher = new _Hexa64Code();
  if (this && this.matchers && this.matchers[0]) {
    this.matchers.push(matcher);
    return this;
  } else {
    let validator = new Validator(null);
    validator._loadMatchers();
    validator.matchers = [matcher];
    return validator;
  }
}

module.exports = Hexa64Code;
